#include<bits/stdc++.h>
#define rep(i, n)for(int i=0; i<n; i++)
using namespace std;
using f=float;
using ll=long long;
using ld=long double;
using ull=unsigned long long;
using vi=vector<int>;
using pii=pair<int, int>;

int main() {
    double d_min = DBL_MIN;
    double d_max = DBL_MAX;
    cout << d_min << endl;
    cout << d_max << endl; 
    cout << sizeof(d_min) << endl;
    return 0;
} 
